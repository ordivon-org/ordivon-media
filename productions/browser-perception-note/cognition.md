# Browser Perception Note — production cognition

Status: active production decision record
Protocol: [`../../research/expression/protocol.md`](../../research/expression/protocol.md)
Profile: [`writing`](../../research/expression/profiles/writing.md)

This is **not a second Production manifest**. It records current writing judgment while `production.json`, `claims.json`, selected evidence, and the owning Web revision retain factual/physical authority.

## FRAME

**Reader model.** Show one ordinary Web case where source/mechanical correctness did not settle the rendered reading decision, then distinguish the narrow Web correction from the broader production-system lesson.

**Non-goal.** Do not turn one browser correction into a generic design rule, a claim about human aesthetic preference, or proof that smaller titles are better.

## BIND

Owner authority is Ordivon Web revision `e11a6585b049776e46011289c80197f1183ec330`. `claims.json` binds the article to Web's retained local observation and desktop-only source revision. `evidence/browser-review.receipt.json` is a selected Studio evidence record, not a replacement Web authority.

The selected before/after evidence preserves exact desktop/mobile model-view digests, mechanical acceptance, and the bounded Agent decision. The mobile before/after digest equality is evidence of the correction's scope, not evidence of universal responsive correctness.

## EXPRESS

Use the Writing prior “specific before abstract”: begin with the apparently-correct page and the browser contradiction before discussing recursive learning or schema design.

Keep three distinctions visible throughout:

1. source/publication correctness versus rendered encounter;
2. local Web observation versus cross-production production-system prior;
3. reinforced existing prior versus genuinely new reusable structure.

The ending should change the reader's model of recursive improvement: evidence can justify refusing to generalize just as much as it can justify promotion.

## RENDER

The current artifact is the complete semantic text in `story.mdx`. No browser publisher is selected for this Production, so Studio audits the semantic text artifact directly and makes **no claim about a final browser reading surface**.

This Writing-only workload intentionally has `workingProfile: {}`. It exercises the corrected Production schema rather than inventing frame rate, canvas, color, or audio values for prose.

## AUDIT

Audit the complete article against the three Web Claims and selected review receipt.

Required semantic checks:

- the desktop defect is described as orientation/publication-identity placement, not factual article failure;
- mobile is described as byte-identical after the desktop-only revision;
- the article does not claim higher beauty/preference;
- “smaller titles are better” is explicitly rejected;
- Web remains owner of the local layout finding;
- the Runtime Introduction comparison is used only to establish repetition of **rendered semantic inspection**, not to merge the two local fixes;
- the Production-schema lesson is about medium applicability, not removing technical profiles from video.

## DECIDE

Current decision: **`approved`, not `published`.** The final source/claim audit passed against exact Web revision `e11a6585b049776e46011289c80197f1183ec330`; all three Claim source paths exist at that revision, the selected evidence preserves the desktop-change/mobile-byte-identity boundary, and the complete semantic text passed the scoped drift checks above. The approved article bytes are `sha256:f6ef709cd0b93a548005b46a2d66af65ceff225f658f11fe91228044753d0ab5`. No browser publisher/distribution surface has been selected, so approval does not imply publication or rendered-browser acceptance.

## LEARNING

Two materially different production pressures now repeat one existing production-system principle:

- Web browser pixels exposed a desktop orientation relation that source/mechanical checks did not settle;
- Studio motion pixels exposed a provenance-label implication that source/type/video QC did not settle.

This **reinforces** the existing requirement for rendered artifact inspection. It does not earn a new Art & Expression Core prior because the principle was already explicit before these cases.

The genuinely new reusable structural change from the second Production is narrower: generic Production metadata must not require video/audio parameters from media that do not use them. `workingProfile` therefore becomes medium-applicable while its individual fields retain validation when present.

### P3 durability falsifier

P3 reused this materially different Writing-only Production to test whether the new media durability rule should become universal CAS policy. It should not. The approved Output digest `sha256:f6ef709cd0b93a548005b46a2d66af65ceff225f658f11fe91228044753d0ab5` exactly matches the current `story.mdx` bytes and the bytes recovered from the committed Git object with `git show HEAD:productions/browser-perception-note/story.mdx`. No matching media-cache object exists, yet the exact approved payload remains recoverable because Git is already its owner-native durable byte authority. Retain the stronger invariant—**exact selected bytes must have a recoverable durable authority**—without requiring one storage mechanism across media.
