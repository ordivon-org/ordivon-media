# Ordivon Book v0 — current production cognition

Status: active four-chapter Book decision record
Profile: [`writing`](../../research/expression/profiles/writing.md)
Constitution: [`constitution.md`](constitution.md)
Source map: [`source-map.md`](source-map.md)
External pressure: [`external-methods.md`](external-methods.md)
Acceptance: [`consumption-audit.md`](consumption-audit.md)

This cognition record owns only current editorial/integration judgment. `production.json`, `claims.json`, exact source bindings, owner-native authorities and exact Output bytes remain stronger.

## FRAME

Book v0 now has four approved chapter consumers:

1. `story.mdx` — finite intelligence from Reality contact through Representation, Evidence/Standing, authorized Action, claim-relative Consequence and Revision.
2. `chapter-02.mdx` — operation/consumer/continuation-relative representation sufficiency, Problem/Solution asymmetry and recoverable re-entry after compression.
3. `chapter-03.mdx` — the constrained transformation from Standing through authority/admission, selection, commitment and effect evidence into claim-relative consequence and re-adjudication.
4. `chapter-04.mdx` — History/Currentness/Recovery: how a finite intelligence re-establishes enough owner-local present relevance to make the next action or non-action decision without converting history, continuity, recency or uncertainty into current authority.

The four chapters remain complementary rather than forming a closed grand theory. Chapter 2 asks what a consumer needs to see and what distinctions remain reachable; Chapter 3 asks how standing can lawfully cross into commitment/effect; Chapter 4 asks whether previously formed representations/standings/commitments still bear on the present action and how decision capability is recovered after time, owner advance or ambiguity.

## BIND

Current permanent claim fences:

- Atlas `c0ba3c7d22e217d3d38553542050aa0360a1e3f8` — non-authoritative cross-owner synthesis/recovery/provenance;
- pre-Book Media `69ab3a4d781ccc6d9718b7e66fc6d34a531a0935` — independent Writing/Production craft authority;
- Chapter-2 Media semantics `710270d9aa4660519f7d6aa3a6007f76aec173ba` — OMPC/owner-first projection semantics;
- Finance `913d7b22b2474465da6a7b6ebcc9406b2b1032dd` — current Decision/Proposal/authority/effect/currentness semantics;
- Normative `68826a3b256da0bba8d540102528721a3e338c25` — normative power/admission owner boundaries;
- Harness `d72413de62c01d7b5124861d9028495707e01d5d` — capability/admission/selection/execution boundary;
- Runtime `fd6765c20981461f1a29508d97225b96971cc389` — physical commitment/effect/evidence/reconciliation boundary;
- Host `b40c7e6a56011dad3ccb595b332ab0edf9fa200a` — semantic continuity and revision-fenced recovery boundary;
- current Media/Book `5b861981c9f63451bace1118e56d407fdfb14109` — Book/OMPC currentness non-fabrication and selective source-revalidation boundary.

The Book now carries 26 Git-bound Claims. A final source gate checked 70 source/evidence paths across their exact binding revisions with zero missing paths.

Chapter 4's central `Actionable Now` is explicitly Book-level integration rather than an owner-native truth class. Its explanatory frontier notation is intentionally open:

```text
D_hat(a,t) = currently discovered load-bearing dependency frontier
U(a,t)     = unresolved dependency / supersession residual
```

Neither is a schema, complete dependency graph, Currentness service or global semantic clock.

## EXPRESS

Chapter 4 must preserve these anti-collapses:

```text
old != wrong
HistoricalValidity != CurrentActionAuthority
Latest != Superseding
Continuity != CurrentWork
Projection != Authority
Recoverable != Actionable
Resume != Re-admission
Reconciliation != Retry
Unknown != Failure != Success
ConsistentGlobalSnapshot != SemanticActionability
```

The chapter's positive synthesis is deliberately weak:

```text
ActionableNow(a,t)
=
a finite agent has recovered enough owner-local evidence,
currentness / authority / admission boundaries,
and explicit unresolved state
to make the next action-or-non-action decision
without projecting historical or unknown coordinates as current.
```

`enough` is load-bearing. The Book does not claim that a finite consumer knows every dependency. Valid recovery outcomes include `ACTION`, `WAIT`, `RESEARCH`, `RECONCILE`, `ABSTAIN`, `BLOCK` or `NO_CHANGE`.

The narrative order is concrete paradox -> mature prior-art subtraction -> heterogeneous owner evidence -> weak formalization -> recovery/UNKNOWN -> Agent–Environment bridge. Agent–Environment remains downstream; no environment manager or automatic affordance compiler is admitted here.

## RENDER

Exact approved semantic artifacts:

- Chapter 1 `story.mdx` — raw SHA-256 `c6835bcf306542801bdcd76cfb32492747d1fb2267f9f8c3164e30303cea1747`.
- Chapter 2 `chapter-02.mdx` — raw SHA-256 `876c2eaec5e15b63aec11d2a3e815c151cc388294f676342225f76c80780d5b9`.
- Chapter 3 `chapter-03.mdx` — raw SHA-256 `7d655614b15302182e65d1b360f003df0bf6f5219555833073fc9c865b41e45f`.
- Chapter 4 `chapter-04.mdx` — raw SHA-256 `ebf36b9d00dfecf170396fd9aac38945476b2d364106e6a5ca65210fa39b6d5b`.
- current four-chapter `book.mdx` — raw SHA-256 `7b7c4473bb3667fabec83f8ca1458c2fb7eddea2418ee80e7529bdeb63f33afe`.

Chapter 1–3 exact bytes remained unchanged while Chapter 4 was added. `book.mdx` contains exactly one Chapter-4 heading and one Chapter-4 TOC entry.

The parent Production remains `review`: no public publisher is selected and Human-specific continuous-reading effects remain unclaimed.

## AUDIT

Current mechanical gate:

- `Studio production models are valid.`
- all 26 Claims are schema-valid under the current Production model;
- all 70 Claim source/evidence paths resolve at exact pinned Git revisions;
- Chapter 1–3 raw digests remain unchanged;
- Chapter 4 and four-chapter combined Output digests match exact raw bytes;
- a final `git diff --check` remains required after this cognition/acceptance writeback;
- no Book schema, service, repository, Edition object, currentness daemon or Agent Tool was added.

### Chapter-4 chapter-only adversarial consumer

Accepted structured Run: `harness-run:book-ch4-structured-1787579670916`.

Input: exact `chapter-04.mdx` only; no repositories, Atlas, source map, research chronology or domain Tools.

Observed:

- provider/model: DeepSeek / `deepseek-v4-flash`;
- stop `candidate_completed`;
- model calls: 2;
- Tools: 0;
- total tokens: 13,931;
- conclusion corrections: 1.

The consumer correctly rejected all targeted collapses: `latest -> current`, historical validity -> actionable now, recoverable -> current authority, resume -> current work, global consistent snapshot -> semantic actionability, response loss -> failure, UNKNOWN -> success/failure, Recovery -> Replay, whole-world refresh before every action, complete dependency-graph assumption, recent render -> source currentness, and Actionable Now -> mandatory external action.

It correctly admitted `NO_CHANGE` and `UNKNOWN` as first-class outcomes and correctly resolved five scenarios: expired Decision -> fresh review; response loss -> reconcile same operation; unrelated newer commit -> no automatic supersession; READY continuity + moved owner -> recover then revalidate owner/current work; unchanged load-bearing boundary -> `NO_CHANGE`.

The single conclusion correction is retained as carrier friction rather than erased.

### Four-chapter whole-Book consumer

Rich long-form structured carrier attempts did not produce a semantic result: one stopped after one conclusion correction with `budget_exhausted`; a reduced but still prose-heavy carrier stopped after two conclusion corrections with `budget_exhausted`. A record-mode free-form Run reached `candidate_completed` but durably retained only a generic summary, so it was not admitted as comprehension evidence.

The final consumer therefore used a smaller role/relationship enum contract rather than weakening semantic criteria.

Accepted Run: `harness-run:book-v0-fourchapter-role-enum-1787580156880`.

- exact four-chapter `book.mdx` only;
- one model call;
- zero Tools;
- 16,667 tokens;
- zero conclusion corrections;
- stop `candidate_completed`.

It recovered:

```text
Chapter 1 = reality-representation-standing-revision-loop
Chapter 2 = operation-relative-representation-and-search-sufficiency
Chapter 3 = standing-authority-commitment-effect-consequence
Chapter 4 = history-currentness-recovery-actionable-present
```

and correctly classified:

```text
Chapter 4 relation to Chapter 2
= adds current action relevance without replacing representation sufficiency

Chapter 4 relation to Chapter 3
= adds temporal revalidation/recovery without replacing authority/effect chain
```

It also held `grandTheoryEstablished=false`, `actionableNowGlobalState=false`, `actionableNowUniversalSchema=false`, `bookOutranksOwners=false`, historical validity -> current action false, representation truth -> operation sufficiency false, Standing -> authority false, Runtime success -> semantic completion false, while preserving `unknownCanRemainActionRelevant=true`, `nonActionOrNoChangeCanBeMature=true`, `openBoundariesRemain=true`, and `researchChronologyNeeded=false`.

## DECIDE

Approve exact Chapter 4 semantic text at raw digest `sha256:ebf36b9d00dfecf170396fd9aac38945476b2d364106e6a5ca65210fa39b6d5b`.

Approve exact current four-chapter `book.mdx` at raw digest `sha256:7b7c4473bb3667fabec83f8ca1458c2fb7eddea2418ee80e7529bdeb63f33afe` as the current integrated semantic-text Output.

Keep the parent Production at `review`.

Do not create a universal Currentness service, global semantic clock, dependency graph, recovery engine, Book Edition object, environment manager, scheduler, new Book/Text repository or service. Four chapters still fit the existing Production + Claims + source fences + exact Output digests model.

## LEARNING

1. Chapter 4 survives prior-art subtraction only as an operation-relative finite-intelligence integration problem; event history, bitemporal time, distributed ordering/snapshot, belief revision, provenance and protocol freshness already have mature owners outside Ordivon.
2. `HistoricalValidity != CurrentActionAuthority` is directly owner-witnessed rather than purely philosophical: Finance preserves expired historical Decisions while requiring fresh review for new action progression.
3. Host demonstrates `Continuity != CurrentWork`; Runtime demonstrates `Reconciliation != Retry` and positive UNKNOWN; Atlas demonstrates retained history without owner-authority minting; Media demonstrates currentness non-fabrication.
4. The chapter's strongest formalization had to be weakened from a complete `D(a)` dependency set to `D_hat(a,t)` plus unresolved `U(a,t)`. Open-world incompleteness is part of the model, not a defect to hide.
5. Selective revalidation is not lazy caching: expiry, owner contracts, changed operation, source-resolution failure, ambiguity and new Reality pressure may trigger reacquisition even without a change notification.
6. Recovery may legitimately produce non-continuation. Restoring decision capability is stronger than restoring inertia.
7. Structured-carrier failures again demonstrate `carrier success != semantic truth` and `carrier failure != semantic failure`; acceptance requires the right consumer contract, not a forced green metric.
8. The current four-chapter Book can bootstrap one independent Agent into all four distinct roles and their non-collapse relations without external research chronology.
9. Chapter 4 now creates a natural but still unearned next seam: recovered currentness can change default context/tool exposure/affordance, which belongs to later Agent–Environment work rather than this chapter.
