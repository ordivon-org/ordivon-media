# Ordivon Book v0 — current production cognition

Status: active three-chapter Book decision record
Profile: [`writing`](../../research/expression/profiles/writing.md)
Constitution: [`constitution.md`](constitution.md)
Source map: [`source-map.md`](source-map.md)
External pressure: [`external-methods.md`](external-methods.md)
Acceptance: [`consumption-audit.md`](consumption-audit.md)

This cognition record owns only current editorial/integration judgment. `production.json`, `claims.json`, exact source bindings, owner-native authorities and exact Output bytes remain stronger.

## FRAME

Book v0 now has three approved chapter consumers:

1. `story.mdx` — finite intelligence from Reality contact through Representation, Evidence/Standing, authorized Action, claim-relative Consequence and Revision.
2. `chapter-02.mdx` — operation/consumer/continuation-relative representation sufficiency, Problem/Solution asymmetry and recoverable re-entry after compression.
3. `chapter-03.mdx` — the constrained transformation from Standing through authority/admission, selection, commitment and effect evidence into claim-relative consequence and re-adjudication.

The three chapters are complementary but deliberately not a closed grand theory. Chapter 2 narrows how Chapter 1's representation distinctions are consumed; Chapter 3 deepens one seam in Chapter 1 without making the open loop deterministic.

## BIND

Current permanent claim fences:

- Atlas `c0ba3c7d22e217d3d38553542050aa0360a1e3f8` — non-authoritative cross-owner synthesis/recovery/provenance;
- pre-Book Media `69ab3a4d781ccc6d9718b7e66fc6d34a531a0935` — independent Writing/Production craft authority;
- Chapter-2 current Media semantics `710270d9aa4660519f7d6aa3a6007f76aec173ba` — OMPC/owner-first projection semantics;
- Finance `913d7b22b2474465da6a7b6ebcc9406b2b1032dd` — current Decision/Proposal/authority/effect semantics;
- Normative `68826a3b256da0bba8d540102528721a3e338c25` — current normative power/admission owner boundaries;
- Harness `d72413de62c01d7b5124861d9028495707e01d5d` — current capability/admission/selection/execution boundary;
- Runtime `fd6765c20981461f1a29508d97225b96971cc389` — current commitment/effect/evidence/reconciliation boundary.

Chapter 3 uses 8 new Git-bound Claims and does not promote mutable Finance live state into permanent source truth.

A separate read-only Finance episode at `2026-08-24T12:16:38.438598Z`, stateVersion `1564:e316d435f391005b`, observed active owner-native capital authority together with an expired historical `abstain` Decision, `needs_revision`, no current Proposal/Effect and a `review-decision-epoch` obligation. This is current consumer pressure for `AuthorityPresent != ActionNow` and `HistoricalDecisionValidity != CommitmentCurrentness`, not a frozen Book claim.

## EXPRESS

Chapter 3 must preserve the following anti-lifting sequence:

```text
Standing
  != practical authority
Authority/current admission
  != selection
Selection
  != commitment/effect occurrence
Execution result
  != external-effect receipt
Effect
  != bounded downstream consequence
Consequence
  != benefit
```

Its negative control is first-class non-action: Finance `research / wait / abstain` are complete Decisions under an explicit NO-OP baseline. `NO_CHANGE` and no external effect can therefore be mature outcomes rather than evidence of an incomplete Agent.

Constitutional rule change, agenda power and institutional legitimacy are intentionally deferred. Agent-Environment/Affordance appears only as a downstream bridge to later work.

## RENDER

Exact approved semantic artifacts:

- Chapter 1 `story.mdx` — raw SHA-256 `c6835bcf306542801bdcd76cfb32492747d1fb2267f9f8c3164e30303cea1747`.
- Chapter 2 `chapter-02.mdx` — raw SHA-256 `876c2eaec5e15b63aec11d2a3e815c151cc388294f676342225f76c80780d5b9`.
- Chapter 3 `chapter-03.mdx` — raw SHA-256 `7d655614b15302182e65d1b360f003df0bf6f5219555833073fc9c865b41e45f`.
- current three-chapter `book.mdx` — raw SHA-256 `ed2dfcf93e4d766f04cebd1ec56962a000dcefce9f8b9544bd7d296f1c1877e7`.

`book.mdx` is a deterministic textual assembly of the three exact chapters plus a minimal title/contents preface. No Edition schema was introduced.

The current combined Output is now approved because an independent whole-book fresh-Agent consumer successfully recovered the three distinct chapter roles, cross-chapter relationships, explicit anti-grand-theory boundaries and open problems from `book.mdx` alone.

The parent Production remains `review`: no public publisher is selected and Human-specific continuous-reading effects remain unclaimed.

## AUDIT

Current mechanical gate before canonical admission:

- `Studio production models are valid.`
- 17 targeted Model/Agent-surface tests pass.
- all 20 Claim source/evidence paths exist at exact pinned revisions.
- Chapter-1 and Chapter-2 approved bytes remain unchanged.
- Chapter-3 and current Book Output digests match exact raw bytes.
- deterministic chapter assembly order is Chapter 1 -> Chapter 2 -> Chapter 3.
- ordinary `studio_production_standing/context` discovers all Outputs and 20 Claims without Book-specific code.

### Chapter-3 free-form durable falsifier

Accepted Run: `harness-run:book-ch3-freeform-1787574323696`.

- DeepSeek `deepseek-v4-flash`;
- one model call;
- zero Tools;
- 6,240 tokens;
- zero conclusion corrections.

It received only Chapter 3 and correctly rejected all 13 targeted lifts: Standing->permission, capability->authority, admission->selection, selection->effect, Runtime success->semantic completion, ActionBlocked->DecisionBlocked, non-action->failure, small effect->small consequence, good outcome->good decision, Human universal gate, more evidence->monotonic freedom, start-current->commit-current, and permission/capability/control interchangeability.

It also correctly adjudicated all five scenarios: no-authority blocks the effect; explicit abstain can be a complete Decision; changed load-bearing currentness before costly commitment requires bounded revalidation; unresolved effect receipt requires reconciliation/UNKNOWN rather than blind retry; admitted but unselected action remains uncommitted. Research chronology was not needed.

One earlier run reached the Harness result but the local dogfood script failed while printing a non-existent telemetry attribute. That is retained as instrumentation failure and carries no Chapter semantic standing.

### Chapter-3 structured carrier

First structured run `harness-run:book-ch3-structured-1787574370812` successfully carried all action-boundary fields in one call/zero corrections, but an ambiguously named `chronologyRequired` field was answered `true` despite chapter-only completion. The field itself was then rewritten to the operationally precise `researchChronologyNeededToAdjudicateTheseCases` without changing Chapter 3 or the action scenarios.

Corrected structured Run `harness-run:book-ch3-structured-1787574411731` completed in one model call / zero Tools / 5,605 tokens / zero corrections. All 13 implication booleans were `false`, all five scenario enums were correct, and `researchChronologyNeededToAdjudicateTheseCases=false`.

This is stronger carrier evidence than the Chapter-2 structured failures and demonstrates that the current Harness structured-result path can durably carry this Chapter-3 audit.

### Three-chapter whole-Book consumer

Run `harness-run:book-v0-threechapter-1787574455214` received only exact `book.mdx`.

- one model call;
- zero Tools;
- 12,916 tokens;
- zero conclusion corrections;
- stop `candidate_completed`.

The consumer correctly recovered:

- each chapter's distinct job;
- Chapter 2 as a refinement of Chapter 1's representation relation rather than replacement;
- Chapter 3 as a deepening of Standing->Reality without deterministic action lifting;
- `universal complete epistemology = FALSE`;
- `Representation determines Reality = FALSE`;
- `Standing + capability is enough for action = FALSE`;
- `Book outranks owner truth = FALSE`;
- `Runtime execution success => beneficial consequence = FALSE`;
- `NO_CHANGE/non-action can be legitimate = TRUE`;
- a valid integrated case where representation is operation-sufficient but action is blocked by authority/currentness;
- multiple explicit open boundaries;
- no external research chronology was required.

The consumer also explicitly preserved unresolved tensions rather than smoothing the chapters into a grand theory.

This supersedes the earlier two-chapter standing `whole-book independent comprehension = UNKNOWN` for the **current three-chapter artifact only**. It does not retrospectively convert the old failed Provider episodes into semantic failures or successes.

## DECIDE

Approve exact Chapter 3 semantic text at raw digest `sha256:7d655614b15302182e65d1b360f003df0bf6f5219555833073fc9c865b41e45f`.

Approve the exact current three-chapter `book.mdx` at raw digest `sha256:ed2dfcf93e4d766f04cebd1ec56962a000dcefce9f8b9544bd7d296f1c1877e7` as an integrated semantic-text Output.

Keep the parent Production at `review`.

Do not create an Action ontology/schema/service, permission engine, authority graph, consequence graph, Book Edition object or new Book/Text repository. Three chapters still fit the existing Production + Claims + source fences + exact Output digests model.

## LEARNING

1. Chapter 3 survives prior-art subtraction only as cross-layer finite-agent action engineering; practical reason, authority, planning and control theory already own the broad conceptual territory.
2. Current owner-native engineering sources now carry stronger action distinctions than historical conversation summaries alone: Finance, Normative, Harness and Runtime independently separate their own authority/effect boundaries.
3. `AuthorityPresent != ActionNow` is not merely theoretical: current Finance showed active owner authority while semantic currentness blocked new action progression.
4. First-class non-action prevents Book/Agent action bias. A mature finite intelligence may decide, wait, abstain, preserve UNKNOWN or return NO_CHANGE.
5. Runtime/Harness evidence makes `physical execution != external effect truth != semantic completion` operational rather than philosophical.
6. Consequence and benefit remain owner/claim-relative; no universal outcome/value terminal is earned.
7. The corrected structured carrier passed Chapter-3 falsification in one call, so Chapter-2 carrier failures should remain local historical transport evidence rather than a permanent assumption about Harness capability.
8. The current three-chapter Book can now bootstrap one independent Agent at the whole-Book level without research chronology while preserving open boundaries and cross-chapter tension.
9. Chapter count still does not earn Edition/schema infrastructure. The next chapter must again be selected by a real integration/consumer pressure, not table-of-contents symmetry.
