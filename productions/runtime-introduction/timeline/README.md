# Editorial timeline

The final editorial cut has not been created yet.

When editing begins, this directory will contain milestone OTIO snapshots such as:

```text
assembly.otio
picture-lock.otio
publication-master.otio
```

The Resolve project export or archive is retained as a binary Asset outside Git. OTIO is an open editorial snapshot, not a promise to preserve every Resolve title, effect, color node, Fairlight process, or generator.
