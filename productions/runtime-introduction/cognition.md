# Runtime Introduction — current production frontier

Status: active production decision record
Protocol: [`../../research/expression/protocol.md`](../../research/expression/protocol.md)
Historical closeout through P4: [`history/cognition-through-p4.md`](history/cognition-through-p4.md)

This file contains only the **current judgment needed to continue the Production**. It is **not a second Production manifest** and not an append-only history. Claims, Assets, TimedText, Receipts, Timeline snapshots, byte authorities, and owning Runtime sources remain authoritative for their facts.

## FRAME

A technically literate viewer should understand one bounded proposition: after uncertain response delivery, reliable Agent execution needs durable operation identity, recoverable evidence, and exact reviewed-state cleanup. The current landscape film is a 78-second picture+narration review candidate; the next decision is only whether its selected synthetic narration is publication-worthy to a human listener.

## BIND

Runtime claims remain bound to Runtime revision `5dd206c74a2b9151fb0a87579ba2200aaf892633` and [`evidence/runtime-demo.receipt.json`](evidence/runtime-demo.receipt.json). Narration text/timing is owned by [`timed-text/narration.en.json`](timed-text/narration.en.json), with generation facts in [`evidence/narration-sapi.receipt.json`](evidence/narration-sapi.receipt.json). Selected-media durability and R2 restore evidence is in [`evidence/r2-replica.receipt.json`](evidence/r2-replica.receipt.json).

## EXPRESS

Keep the established provenance grammar: receipt-derived evidence views, deterministic explanatory motion, and clearly labeled Studio framing. The final tail remains `65–67 Diff → 67–74 Boundary → 74–78 End`; physical voice materialization already falsified the older timing, so do not restore it merely for historical consistency.

## RENDER

The selected landscape Output is `sha256:7d994f80627968f4e64a3a53c08d5241bb8f398e17d52c24080f935e7c716430`, 78.000 seconds, H.264 BT.709 + AAC 48 kHz mono. The exact audition copy is `D:\\OrdivonStudio\\productions\\runtime-introduction\\review\\runtime-introduction-en-7d994f806279.mp4`. [`timeline/assembly.v2.otio`](timeline/assembly.v2.otio) is the current A/V review snapshot.

## AUDIT

Machine evidence is already sufficient for source/claim binding, exact bytes, cue fit, A/V structure, loudness/peak, local recovery, and destructive off-machine restore. Do **not** reopen those questions without contradictory evidence. One unresolved claim remains: whether Microsoft Zira Desktop at the selected timing sounds natural, intelligible, appropriately paced, and publication-worthy to a human listener. Hashes, waveform facts, ffprobe, or Agent preference cannot settle that claim.

Routing remains narrow: wrong Runtime fact → `BIND`; wrong narration meaning → `FRAME/EXPRESS`; timing/cut/level defect → `RENDER`; voice naturalness → bounded human audition.

## DECIDE

Keep `runtime-film-en-landscape` at Output status `rendered` and the whole Production at `review`. Do not promote the film to `approved` until the exact selected Blob receives the bounded human audition. If rejected, replace only the narration voice/provider and rerun cue-fit → archive/replicate → mux → artifact audit; do not reopen Production architecture without new evidence.

## LEARNING

Retain only active reusable priors here: selected bytes need recoverability at the claimed failure boundary; physical medium evidence may revise abstract editorial locks; human response is a conditional sensor rather than a universal approval stage; the current Cloudflare R2 path is a single-writer verified replica, not a general storage platform. Detailed P2/P3/P4 findings remain in the frozen historical snapshot and selected Receipts rather than accumulating in this frontier.
