# Ordivon Root Social Card

A static 1200×630 social-preview Production for the Web root OpenGraph/Twitter surface.

- `production.json` — lifecycle/output decision.
- `claims.json` — source-bound Web copy and visual-language claims.
- `assets.json` — exact editable SVG and rendered PNG identities.
- `source/opengraph-image.svg` — editable vector authority for this Production.
- `output/opengraph-image.png` — current rendered candidate.
- `cognition.md` — compact FRAME→DECIDE frontier.

The Web repository remains owner of metadata and publication. Studio owns this production decision and exact candidate bytes until Web deliberately consumes them.
