# Ordivon Media Foundations — MF0→MF7 Closeout

**Date:** 2026-08-17  
**Status:** MF0–MF7 frozen as current v1 substrate  
**Next frontier:** MF8-A — Agency Ontology & Term Separation  
**Continuity Task:** `task:media-foundations-mf2h-20260817`

---

# 0. Purpose of this closeout

This file is the compact durable bridge from the completed MF0→MF7 foundation program into MF8. It is not a replacement for the canonical round files. It records the current frozen substrate, the non-collapse rules that must survive future work, the exact next frontier, and the FoundationReopen discipline.

MF0→MF7 should **not** be re-researched from scratch in a new conversation. Reopen only when a concrete FoundationReopenCondition is triggered.

---

# 1. Current authority status

```text
MF0 — Media Ontology                         FROZEN
MF1 — Signal Foundations v1                 FROZEN
MF2 — Perception Foundations v1             FROZEN
MF3 — Representation Foundations v1         FROZEN
MF4 — Composition Foundations v1            FROZEN
MF5 — Space Foundations v1                  FROZEN
MF6 — Time Foundations v1                   FROZEN
MF7 — State & Dynamics Foundations v1       FROZEN

NEXT — MF8 Agency Foundations
        MF8-A Agency Ontology & Term Separation
```

Canonical MF7 closeout:

`research/expression/foundations/media-foundations-mf7h-20260817.md`

---

# 2. Research discipline that remains binding

1. Use first-principles decomposition before engineering consumption.
2. Treat overloaded concepts as typed relations/profiles rather than unqualified booleans/scalars.
3. Separate phenomenon, model/computational description and implementation.
4. Cross-check technical claims against primary/authoritative literature.
5. Falsify before freeze.
6. Preserve competing theories where evidence does not decide.
7. Preserve source evidence, inference, uncertainty and provenance separately.
8. Do not silently transfer standing from representation/formal model to target reality.
9. Do not prematurely map provisional foundations into Studio/Web/Game/XR.
10. Frozen means current stable substrate, not eternal metaphysical truth.

---

# 3. MF0 — Media Ontology

Minimal media-role structure:

```text
Distinction
+ Structured Mediation
+ Potential Functional Uptake
```

A medium is relational/contextual rather than a natural object class.

Canonical mediation episode:

```text
E = <Distinction, Carrier, Transformation, Coupling, Recruitment,
     Agent/System, Context, Time>
```

Optional rather than universal:

- representation;
- artifact;
- persistence;
- network;
- conscious observer;
- intentional sender;
- agency.

Core firewall:

```text
Medium ≠ Artifact ≠ Representation ≠ Signal ≠ Agent
```

---

# 4. MF1 — Signal Foundations

General signal:

```text
x : D → V
```

Signal role requires structured variation that can be discriminatively recruited.

Critical separations:

```text
Reality ≠ Signal
Signal ≠ Measurement
Signal ≠ Symbol
Signal ≠ Meaning
Sampling ≠ Recovery
Aliasing = Non-identifiability under an observation/source model
Injectivity ≠ Stability
Quantization ≠ Precision ≠ Trueness ≠ Accuracy ≠ Uncertainty
Noise Model ≠ Adversary/Security Model
Recovery ≠ Inference ≠ Generation
Signal Fidelity ≠ Perceptual Fidelity ≠ Task Fidelity
```

Final view:

> Signal world = distinction flow through selective, lossy/stochastic, resource-bounded transformations.

---

# 5. MF2 — Perception Foundations

Frozen core:

> Perception is stateful, selective, potentially action-coupled organization/use of sensorimotor evidence that changes structured discriminability among world/body-relative possibilities under uncertainty.

Possible organizations include objects, events, surfaces, fields, streams, ensembles, body/self and relations.

Critical separations:

```text
Perception ≠ Signal
Perception ≠ Recognition
Perception ≠ Awareness
Physical Geometry ≠ Signal Geometry ≠ Perceptual Geometry
Salience ≠ Priority ≠ Attention
Attention ≠ Gaze ≠ Awareness
```

MF2-I rejected a universal object unit and strong universal latent-source assignment.

---

# 6. MF3 — Representation Foundations

Final representation core:

> A representation is a grounded standing-in relation in which distinctions/structure of a vehicle/type are established through systemic use, design, convention, production history, keyed structure or other non-arbitrary grounding as standing for/specifying/proxying distinctions, conditions or possibilities in a target domain under a context/mode/scope that supports typed evaluation such as reference, fit, fidelity, satisfaction, calibration or error.

Standing model:

```text
RepStanding(V, Θ, D, Φ, M, G, K, Σ, H)
RepActive = RepStanding + U_t + C_t
```

Critical separations:

```text
Information ≠ Representation
Decodability ≠ Content
Content ≠ Referent ≠ Vehicle
Geometry ≠ Semantics
Model ≠ World Copy
Formal Symbol ≠ Semantic Symbol
Representational Standing ≠ Active Use
Analyst Decodability ≠ Representation
```

This representation firewall is consumed throughout MF4–MF7.

---

# 7. MF4 — Composition Foundations

Final core:

```text
Composition
 = Differentiated Multiplicity
 + Typed Organization Standing
 + Nontrivial Joint Organization
 + Organization-Sensitive Profile
 + Scope
```

Canonical standing:

```text
CompositionStanding = <D,U,B,O,S,Φ,Σ,H,Eq,Unc>
```

Critical separation:

```text
AnalystGrouping ≠ Composition
```

Composition provides the non-arbitrary organization substrate later required for collective state/dynamics.

---

# 8. MF5 — Space Foundations v1

Decisive primitive:

```text
PositionalStanding(P, X | Σ)
```

Final compact core:

```text
Space
 = Positional Possibility Domain
 + Positional Standing
 + Typed Relation Structure
 + Standing Route
 + Scope
```

Deepest firewall:

```text
StructuralIsomorphism ≠ OntologicalRoleIdentity
```

Abstract graph/topology/metric/order structure does not become spatial merely because it is drawable or geometrically embeddable.

Critical separations:

```text
StateSpace ≠ PhysicalSpace
AnalystEmbedding ≠ SpatialStanding
Coordinates ≠ Space
Metric ≠ Space
```

---

# 9. MF6 — Time Foundations v1

Decisive primitive:

```text
OccurrenceStanding(X, D | Σ)
```

Final compact core:

```text
Time / TemporalDomain
 = Occurrence-Possibility Domain
 + OccurrenceStanding
 + Typed Temporal Relation Standing
 + Standing Route
 + Scope
```

Critical firewall:

```text
AbstractOrder ≠ OccurrenceStanding
StateTransitionPossibility ≠ TemporalHistory
State ≠ TemporalPosition
TransitionRelation ≠ TemporalOrder
Dynamics ≠ Time
```

Additional key separations:

```text
VersionPrecedence ≠ RevisionOccurrenceTime
WorkflowDependency ≠ ExecutionHistory
ParameterProgression ≠ TemporalProgression
Clock ≠ ClockReading ≠ TimeUnit ≠ TargetTime
CoordinateTime ≠ ProperTime
LogicalClock ≠ PhysicalClock
EventTime ≠ ProcessingTime ≠ PresentationTime
SimulationTime ≠ WallClockTime
```

MF6 is the temporal substrate for MF7 state occurrences, trajectories, histories and persistence.

---

# 10. MF7 — State & Dynamics Foundations v1

MF7-A→G built provisional state/dynamics layers; MF7-H performed final adversarial falsification and **changed two provisional cores before freeze**.

## 10.1 Final State core

Final primitive:

```text
ConfigurationStanding(C, B | Σ)
```

A distinction/structured assignment has ConfigurationStanding when it is non-arbitrarily constituted, operationally recruited, represented with grounding, or target-grounded as specifying which admissible condition/configuration alternative of a scoped bearer/system obtains at the declared occurrence/validity scope.

Final compact definition:

```text
State
 = Bearer/Boundary
 + Admissible Configuration Possibility Domain
 + ConfigurationStanding
 + Granularity/Equivalence
 + StandingRoute
 + Occurrence/Validity Scope
```

The earlier provisional requirements `EndogenousConditionStanding` and constitutive `Evolution/Output Relevance` were rejected as too restrictive.

Important:

```text
State ≠ Markov-Sufficient State
State ≠ Complete Reality
```

Prediction, behavioral sufficiency, observability and control usefulness are profiles, not state constitution.

## 10.2 State standing ladder

```text
S0 — nominal `state` vocabulary only
S1 — descriptor/statistic/metadata candidate
S2 — formal/operational StateStanding
S3 — grounded representational StateStanding
S4 — independently evidenced/constituted target StateStanding
```

Formal state does not automatically transfer to target physical state.

## 10.3 Final Dynamics core

Final primitive:

```text
EvolutionStanding(E, D | Σ)
```

A rule/relation/field/kernel/operator/constraint structure has EvolutionStanding when grounded as determining, constraining, weighting or generating admissible continuation among state/process occurrences under declared boundary/input/disturbance/control and temporal/occurrence semantics.

Final compact definition:

```text
Dynamics
 = State and/or Process Domain
 + EvolutionStanding
 + Typed Continuation Structure
 + Boundary/Input/Disturbance/Control Semantics
 + Temporal/Occurrence Scope
 + StandingRoute
```

Critical separations:

```text
Dynamics ≠ Sequence
Dynamics ≠ Order
Dynamics ≠ Dependency
Dynamics ≠ Trajectory
Dynamics ≠ History
Dynamics ≠ Log
Dynamics ≠ Cause
```

Deterministic, stochastic, nondeterministic, hybrid, set-valued and history-dependent dynamics are all admissible.

## 10.4 Determinism / stochasticity / memory

Frozen distinctions:

```text
Deterministic ≠ Predictable ≠ Reversible
Nondeterministic ≠ Stochastic
Stochastic ≠ Unstructured
Noise ≠ Randomness
Process Noise ≠ Measurement Noise ≠ Parameter Uncertainty
State Uncertainty ≠ Dynamics Stochasticity
Markov ≠ Stochastic ≠ Stationary
Markov Property ≠ Memoryless Reality
NonMarkov ≠ Fundamental Memory
```

State sufficiency/Markov closure is always model/boundary/horizon/consumer relative.

## 10.5 Identity / persistence

Final compact core:

```text
Persistence
 = Bearer Occurrences
 + ContinuationStanding
 + IdentityCriterion
 + Identity-Preserving Transformation Rules
 + Branch/Fusion/Termination Semantics
 + Provenance
 + Temporal Scope
```

Critical firewall:

```text
Continuation ≠ Strict Identity
State Similarity ≠ Token Identity
Persistence ≠ State Constancy
Same State Again ≠ Same State Occurrence
```

Fork/exec, restore, replica, component turnover and branching cases are explicitly supported.

## 10.6 Stability / invariants / regimes

Frozen distinctions:

```text
Observed Constancy ≠ Invariant Standing
Invariant ≠ Constant State
Conserved Quantity ≠ No Dynamics
Equilibrium ≠ Stability ≠ Attractor
Fixed Point ≠ Attractor
Stationary ≠ Static
Stability ≠ Robustness ≠ Resilience ≠ Homeostasis
Attractor ≠ Goal
Regime ≠ State
Bifurcation ≠ State Transition Occurrence
```

These are dynamics profiles, not State constituents.

## 10.7 Final Control core

MF7-H replaced agency-like `SelectableInfluence` wording.

Final compact definition:

```text
Control
 = Controller-Governed / Conditionally Settable Influence
 + Access/Authority
 + Admissible Control Set
 + Effective Actuation Route
 + Target Evolution Relevance
 + Boundary/Scope
```

Critical separations:

```text
Cause ≠ Input ≠ Control
Control ≠ Feedback
Control ≠ Intervention
Control ≠ Policy
Control ≠ Goal
Control ≠ Agency
Command ≠ Actuation ≠ Target Effect
Authority ≠ Capability ≠ Action ≠ Effect
Plant Dynamics ≠ Controller Dynamics ≠ Closed-Loop Dynamics
Reachability ≠ Controllability ≠ Feasibility
Controllability ≠ Stabilizability ≠ Observability
```

This is the hard MF7→MF8 boundary.

## 10.8 Macro / collective state

Final compact definition:

```text
MacroState
 = Collective Bearer/Boundary
 + Coarse Configuration Possibility Domain
 + ConfigurationStanding at Declared Scale
 + Coarse Map/Equivalence
 + StandingRoute
 + Occurrence/Validity Scope
```

Critical:

```text
MacroStateStanding ≠ MacroMarkovSufficiency ≠ DynamicClosure
```

Arbitrary analyst aggregates remain descriptors until formal/target configuration standing is established.

## 10.9 Collective / emergent dynamics

Frozen distinctions:

```text
JointDynamics ≠ Σ IsolatedSubsystemDynamics
Coupling ≠ Control
Interaction ≠ PairwiseEdge universally
NetworkTopology ≠ Dynamics ≠ History ≠ CausalGraph
Synchronization ≠ SameState ≠ Simultaneity
Coordination ≠ Synchronization ≠ SharedGoal
EffectiveDynamics ≠ Micro/FundamentalDynamics by identity
Emergence ≠ Mystery ≠ Unpredictability ≠ FundamentalIrreducibility
SelfOrganization ≠ Agency
CollectiveBehavior ≠ CollectiveGoal
```

Micro/macro are relative scales, not reality ranks.

---

# 11. MF7 v1 frozen axioms

MF7-H freezes twenty high-level axioms:

1. Scoped Configuration
2. Configuration Standing
3. Representation Firewall
4. Sufficiency Is Optional
5. Typed Multi-Standing
6. Evolution Standing
7. No Syntax Collapse
8. Dynamics Plurality
9. Law/Realization Separation
10. Boundary First
11. Identity Separation
12. Stability Family
13. Control Standing
14. Closed-Loop Composition
15. Coarse Standing
16. Scale Relativity
17. Collective Organization
18. Emergence Without Magic
19. Uncertainty/Provenance
20. No Agency Smuggling

---

# 12. Foundation reopen discipline

MF0–MF7 are frozen. Do not reopen because a later engineering consumer is inconvenient.

MF7 reopen only on concrete failure classes such as:

```text
FRC-S1/S2/S3 — State/configuration/representation failures
FRC-D1/D2/D3 — Evolution/dynamics/time-boundary failures
FRC-O1       — open/non-Markov failure
FRC-I1       — identity/persistence failure
FRC-ST1      — stability/regime failure
FRC-C1       — control failure
FRC-M1/M2    — macro standing/scale transfer failure
FRC-A1       — MF8 agency necessarily breaks MF7 core
FRC-X1       — cross-domain empirical counterexample
```

Absent one of these, MF7 v1 stays frozen.

---

# 13. What MF8 must NOT do

MF8 must not silently redefine frozen MF7 primitives in order to make Agency easy.

Especially preserve:

```text
Agent ≠ Controller
Agency ≠ Control
Action ≠ Transition
Goal ≠ Attractor
Goal ≠ Setpoint
Policy ≠ Intent
SelfOrganization ≠ Agency
Coordination ≠ CollectiveAgency
CollectiveBehavior ≠ CollectiveGoal
```

MF8 may enrich State/Control profiles but must not smuggle goal/value/free-choice semantics back into MF7 unless an explicit FRC-A1 is demonstrated.

---

# 14. Next frontier — MF8-A

Start directly with:

```text
MF8-A — Agency Ontology & Term Separation
```

Initial term set:

```text
Agent
Agency
Action
Action Ownership
Choice
Decision
Goal
Need
Want
Preference
Utility
Value
Policy
Plan
Autonomy
Initiative
Intentionality
World Model
Self Model
Learning
Adaptation
Responsibility
Collective Agency
```

Primary opening question:

> **What makes a stateful, controlled, adaptive system genuinely acquire AgencyStanding rather than merely exhibiting control, regulation, goal-shaped trajectories, self-organization or externally interpreted purposiveness?**

First attacks:

```text
Agent ≠ Controller
Agency ≠ Control
Action ≠ StateTransition
Goal ≠ Attractor/Setpoint
Policy ≠ Intent
Adaptation ≠ Agency
Learning ≠ Agency
SelfOrganization ≠ Agency
CollectiveCoordination ≠ CollectiveAgency
```

---

# 15. New-conversation resume instruction

A new conversation can resume with only:

> **继续 Ordivon Media Foundations。读取 `task:media-foundations-mf2h-20260817` 和 repo 中冻结的 MF0–MF7 foundations，从 MF8-A Agency Ontology & Term Separation 开始。不要重做 MF0–MF7；只有出现具体 FoundationReopenCondition 才重新打开。**

Expected durable source head at this closeout's parent is the MF7 freeze commit; revalidate Runtime/Host before any new mutation.

---

# 16. Final closeout status

```text
MF0→MF7 research program: CLOSED/FROZEN as current v1 substrate
Engineering consumption: NOT implied by freeze
MF8 Agency: NOT STARTED in this conversation
Next action: open a new conversation and begin MF8-A
```
