{
  "name": "ordivon-studio",
  "version": "0.1.0-alpha.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=22.12 <23"
  },
  "scripts": {
    "tokens:build": "node --experimental-strip-types packages/identity/scripts/build-tokens.ts",
    "models:check": "uv run python -m ordivon_studio.models",
    "preview:dev": "pnpm --filter @ordivon/studio-preview dev",
    "preview:build": "pnpm --filter @ordivon/studio-preview build",
    "motion:studio": "pnpm --filter @ordivon/studio-motion studio",
    "motion:render:runtime": "pnpm --filter @ordivon/studio-motion render:runtime",
    "motion:qc:runtime": "uv run ordivon-studio qc-video out/runtime-flow.mp4 --width 1920 --height 1080 --frame-rate 30/1 --codec h264 --pixel-format yuv420p --color-space bt709 --color-range tv --no-audio",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "python:test": "uv run python -m unittest discover -s tests -v",
    "check:core": "pnpm tokens:build && pnpm models:check && pnpm python:test",
    "check": "pnpm check:core && pnpm typecheck && pnpm preview:build"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "@types/react": "19.2.17",
    "@types/react-dom": "19.2.3",
    "typescript": "6.0.2"
  }
}
